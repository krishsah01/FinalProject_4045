# ===============================
# DataSource configuration
# ===============================
spring.datasource.url=jdbc:mysql://localhost:3306/roomieRadarData?createDatabaseIfNotExist=true
spring.datasource.username=roomie_user
spring.datasource.password=RoomieRadar!2025
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# ===============================
# JPA / Hibernate
# ===============================
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# ===============================
# Flyway configuration
# ===============================
spring.flyway.enabled=true
spring.flyway.url=jdbc:mysql://localhost:3306/roomieRadarData?createDatabaseIfNotExist=true
spring.flyway.user=roomie_user
spring.flyway.password=RoomieRadar!2025

# ===============================
# Server
# ===============================
# Default is 8080; keep as-is unless they want to change
# server.port=8080
